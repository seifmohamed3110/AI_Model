{
  "permissions": {
    "allow": [
      "Bash(flutter pub *)",
      "Bash(python -c \"import flask; import fitz; import docx; import sklearn; print\\('Dependencies OK'\\)\")",
      "Bash(dir models *)",
      "Bash(curl -s http://localhost:5000/health)"
    ]
  }
}
